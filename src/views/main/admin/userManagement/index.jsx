// material-ui
import { useTheme } from '@mui/material/styles';
import { Grid, Stack, Box, Typography } from '@mui/material';

const userManagement = () => <Typography>userManagement</Typography>;

export default userManagement;
